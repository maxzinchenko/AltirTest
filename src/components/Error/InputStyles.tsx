import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorContainer: {
    paddingVertical: 8,
    paddingHorizontal: 16,
    flex: 1,
    borderRadius: 8,
    backgroundColor: 'rgba(255, 45, 88, 0.05)'
  },

  errorText: {
    fontSize: 12,
    color: '#FF2D58'
  }
});
