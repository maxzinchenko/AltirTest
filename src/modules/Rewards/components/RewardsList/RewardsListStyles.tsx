import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  rewardsContainer: {
    paddingTop: 16,
    paddingBottom: 64
  },

  rewardsList: {
    paddingHorizontal: 16,
    backgroundColor: '#FFF'
  },

  rewardNotFirst: {
    marginTop: 16
  }
});
